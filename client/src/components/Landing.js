import React from 'react';

//landing component function - return JSX
const Landing = () => {
    return (

        <div style={{ textAlign: 'center'}}>
            <h1>Emaily</h1>
            Collect feedback from your users
        </div>

    );
};

export default Landing;